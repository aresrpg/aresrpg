# CODEOWNERS — every path defaults to the owner; the explicit rows exist to make the fortress
# surfaces visible to future maintainers, not to change coverage.
*                   @Sceat

# Contracts are money.
/packages/move/     @Sceat
# The deterministic core + the replay gate — the correctness spine.
/packages/sim/      @Sceat
# Workflows are the rails (the inviolable gate lives here).
/.github/           @Sceat
# The frozen-node constitution.
/FROZEN.md          @Sceat
# The identity class.
/LICENSE            @Sceat
/CLA.md             @Sceat
/TRADEMARK.md       @Sceat
