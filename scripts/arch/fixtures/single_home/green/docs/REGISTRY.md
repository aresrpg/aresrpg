# Reuse registry — GREEN self-test fixture

Both rows anchor a real declaration, and neither fact is declared anywhere but its home.

| Fact domain            | Canonical home                                                        |
| ---------------------- | --------------------------------------------------------------------- |
| Test protocol constant | `packages/alpha/src/protocol.js:5` — the fixture's exported constant. |
| Test scaling helper    | `packages/alpha/src/protocol.js:7` — the fixture's exported helper.   |
