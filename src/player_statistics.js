// this file is intended for general helper computation about the player stats

export const get_max_health = state => {
  return 40
}

export const get_remaining_stats_point = state => {
  return 12
}
