4b5660f74 → Refs #1233 — fix(frontend): item icons resolve presence through the manifest, never a blind GET
be296e4bc → Refs #1233 — fix(frontend): the mob-icon name has one derivation
b9e742351 → Refs #1233 — fix(frontend): spell_icon_url throws on an object-id key like its sibling
907ddac93 → Refs #1233 — fix(frontend): character rigs resolve from their real home — the legacy exception dies
29095dd5d → Refs #1233 — fix(engine): the demo avatar imports a rig that exists
143f68f4c → Refs #1233 — fix(engine): a 404 rig renders a loud fallback, never an invisible ghost
97e545feb → Refs #830 — test(engine): the baker/renderer stragglers get measured budgets
d72782331 → Refs #830 — test(engine): ring_manager budget matches its measured cost
0e688d46c → Refs #830 — test(engine): world-gen suite budgets measured under load
f1dcebe54 → Refs #830 — test(scripts): the ceremony subprocess gets its measured budget
12258d6dd → Refs #2069 — fix(frontend): hud drops mutating methods
262a6f577 → Refs #2069 — fix(frontend): hud params are never reassigned
57e6104b5 → Refs #2069 — fix(frontend): hud prefer-tacit drained
ad3cff6b1 → Refs #2069 — fix(frontend): hud complexity drained
1f4ca167d → Refs #1368 — fix(frontend): the dungeon grid imports its geometry, the twin dies
d4bca32bc → Refs #1368 — fix(frontend): one home for the day-cycle constants
cae4f84de → Refs #1368 — fix: the effect phase table has one home
eecb8c86c → Refs #1368 — fix(frontend): sui_mist grammar written once
7c82136cf → Refs #1368 — fix(fight): one turn-deadline fallback
0e012d1f3 → Fixes #2280 — fix(frontend): an armed weapon paints its target cell red
73de120bf → Refs #1747 — test(sim): pin the AoE caster-exclusion truth for #1747
e636a4b5b → Refs #1747 — test(sim): record the chain-vs-blob evidence for #1747
bd8a67e0f → Fixes #1747 — test(sim): the republished corpus carries the enemies-only filter — #1747's red flips green
fd4ec8a66 → Refs #1233 — test(frontend): the two new icon suites live where tests live
3380c9046 → Refs #1233 — test(sdk): the host-default pin publishes an inventory to resolve against
89276af23 → Refs #1233 — test(frontend): the cloak-art render publishes the class it actually reads
816075ab7 → Fixes #2279 — fix(frontend): the weapon attack bears its item's name
