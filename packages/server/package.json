{
  "name": "@aresrpg/server",
  "version": "1.0.0",
  "private": true,
  "description": "The one realtime door — zkLogin-verified websocket serving the indexer's graph and relaying player events. Writes nothing.",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@mysten/sui": "2.23.2",
    "falkordb": "^6.2.07",
    "ioredis": "^5.9.3",
    "pino": "^10.1.1",
    "@aresrpg/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/bun": "^1.3.5"
  }
}
