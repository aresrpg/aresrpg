// SPDX-License-Identifier: LicenseRef-AresRPG-Source-Available
// © 2026 Sceat — All rights reserved. See LICENSE.
// The authored GLB remains absent from this public repository, but frontend/bunfig.toml preloads the
// same resolver used by Vite: the engine's static `?url` import becomes the runtime CDN route. Keep the
// established availability name for the affected suites; it now describes whether the import is
// resolvable in this runner, not whether private art happens to exist on disk.
export const SENSHI_MALE_GLB_AVAILABLE = true
