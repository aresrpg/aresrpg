{
  "name": "@aresrpg/rpc-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only HTTP JSON API for the AresRPG RPC — serves indexer views from Redis.",
  "scripts": {
    "start": "bun run server.js",
    "dev": "bun --watch server.js"
  },
  "dependencies": {
    "@sentry/node": "^10.67.0"
  }
}
