# AresRPG Claude entrypoint

The repository has one working agreement and one architecture model:

1. Read `AGENTS.md`.
2. Read `ARCHITECTURE.md` completely.
3. Search `DECISIONS.md` for the domain being changed.

Do not create a Claude-specific architecture or workflow. `AGENTS.md` owns working rules;
`ARCHITECTURE.md` owns the current system; `DECISIONS.md` owns active motives.

The detailed TypeScript law remains here because its rule IDs are cited by ESLint:

@.claude/rules/code-law.md
